import React from 'react';

import { Wrapper } from './styles';

const SectionFilters = ({ children }) => (
  <Wrapper className="section-filters">{children}</Wrapper>
);

export default SectionFilters;
