import styled from 'styled-components';
import { CardBody } from '../reactstrap';

const Wrapper = styled.div`
  text-align: center;
`;

const StyledCardBody = styled(CardBody)`
  padding: 0.6rem;
`;

export { Wrapper, StyledCardBody };
